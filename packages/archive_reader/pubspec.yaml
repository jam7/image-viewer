name: archive_reader
description: Range Read based archive reader. Extract individual entries from ZIP without downloading the entire file.
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  logging: ^1.2.0

dev_dependencies:
  archive: ^4.0.2
  test: ^1.24.0
