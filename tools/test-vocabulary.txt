# Vocabulary for test data and documentation examples.
#
# This repository is public; the developer's real library is not. Test data is
# therefore *declared* rather than improvised: anything that looks like content
# -- a path with separators, a media filename, any CJK text -- has to be built
# from the names below, and tools/check-private.py enforces it.
#
# The point is the failure mode it catches. A denylist can only stop names
# someone remembered to list; this stops the work title nobody knew about,
# which is the case that kept slipping through.
#
# Adding to this file is expected and cheap. What is not allowed is quietly
# pasting a name off a device log. If a new name is needed, invent one and put
# it here -- the declaration is the review.
#
# Lines starting with ~ are regular expressions matched against a whole literal
# or a single path segment. Everything else is an exact path segment or file
# name. `#` starts a comment.

# -- SMB-ish share layout ---------------------------------------------------
books
series
vol2
sub dir
subdir
inner
deep
other
leading
images
path
a
b
c
2

# Segments carrying a character that means something in a URI. These exist to
# prove the encoding survives a round trip, so the odd shape is the test.
b#c
b?c
b%c
b&c=d
b+c

# -- file names -------------------------------------------------------------
pic.jpg
file.jpg
file.png
a.jpg
inner.jpg
作品集第2巻.pdf
# An extension the app cannot open, for the test that says so.
a.txt
# A folder whose name has a dot in it -- the case that used to be read as a
# file (gallery_uri_dialect.dart itemOf).
vol2.5

# Names whose shape is the subject of the test (natural sort, paging), where
# the digits are the point and carry nothing else.
~^\$?[a-z0-9_()$-]+\.(jpg|jpeg|png|pdf|zip|mp4)$
~^[0-9]+([.-][0-9]+)*\.(jpg|png)$
~^chapter[0-9]+_page[0-9]+\.jpg$

# -- hosts and URLs ---------------------------------------------------------
# Synthetic hosts only. i.pximg.net appears because the code under test builds
# and retries those URLs; the paths after the host must still be invented.
server
share
~^https://example\.invalid/.*$
~^https://i\.pximg\.net/(stale|fresh)\.jpg$
~^https://i\.pximg\.net/thumb/\$i\.jpg$
~^smb://server/share/.*$
~^smb:test:.*$
~^smb://<[a-zA-Z]+>/.*$

# The app's own URI schemes. A full address splits into segments, and the
# scheme is the first of them -- allowing it does not weaken anything, since
# every segment after it still has to be a name from this file.
smb:
pixiv:
fav:
home:

# A Dart variable holding the id above, as it appears inside an interpolated
# string literal in a test. A name in the source, not data about anything.
$configId

# -- ids --------------------------------------------------------------------
# One invented SMB config id. Real ones are 13-digit creation timestamps, so
# the shape has to stay while the value must not be a real one.
1700000000000

# -- invented people --------------------------------------------------------
# Authors and users shown in the viewer. Deliberately not plausible as real
# pixiv account names.
test-user
テスト作者

# -- UI strings that tests match on -----------------------------------------
# These are labels the app itself renders, asserted by widget tests. They are
# the app's own words, not data about anything the developer owns.
お気に入り
サービス
サーバー
ライブラリ
ブックマーク
設定
ホーム
再読み込み
の作品
新しいタブで開きました:
ファイルが見つかりませんでした
画像が見つかりませんでした
読み込めませんでした。タップで再試行
# How the toolbar names a search whose options are not the usual ones
# (gallery_uri_dialect.dart). They annotate a word from this file, so the
# whole label has to be allowed as well -- hence the alternation below.
部分
古い順
# The same switches as they read in the window, including the default side
# (gallery_uri_dialect.dart searchOptions).
完全
新着
トップ
アドレスをコピー
# What an empty required field in the SMB connection form says.
必須
~^[^\n]*(お気に入り|サービス|サーバー|ライブラリ|ブックマーク|設定|ホーム|再読み込み|の作品|部分|古い順|見つかりませんでした|新しいタブで開きました)[^\n]*$

# -- documentation examples -------------------------------------------------
# Paths in the developer commands in CLAUDE.md. Written as an environment
# variable so no real user directory appears.
$env:USERPROFILE
flutter_log.txt
~^smb:1700000000000:[A-Za-z0-9/._-]+$

# -- platform and protocol constants ----------------------------------------
# Not data about anything the developer owns.
%LOCALAPPDATA%
flutter_webview_windows
image_viewer
116444736000000000            # Windows FILETIME epoch offset, in 100ns ticks
NTLMSSP\0                     # the NTLM signature, a fixed byte string

# -- placeholders in prose and commit messages ------------------------------
~^(full|thumb):smb:\.\.\.:.*$
~^N(-N)*\.(jpg|png)$

# -- names that appear only in already-published history --------------------
# Both are invented: placeholders left by the 2026-07-25 sanitize of the
# benchmark doc comment, and an example cache key. Neither was ever declared
# here, so every audit reported them as unknown content. Declaring them
# removes the noise without hiding anything -- the decision to leave the
# published history as it is, and why, is in docs/private-data.md.
photos
~^smb:123:path/file\.jpg$
